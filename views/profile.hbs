<div class="profile-section">
  <section class="container-head">
        <div class="img-profile">
          {{#if currentUser.imgUrl}}
          <img src="{{currentUser.imgUrl}}" alt="" width="140" height="140">
          {{/if}}
          {{#unless currentUser.imgUrl}}
          <img src="/images/profile-pic.jpg" alt="" width="140" height="140">
          {{/unless}}
        </div>
        <div class= 'user-email-name'>
          <h2>{{currentUser.username}}</h2>
          <p class="profile-email">{{currentUser.email}}</p>
        </div>
  </section>
  <div class="services-section-profile">
    <section class="container">
        {{#unless currentUser.imgUrl}}
          <form action="/profile/upload" method="post" enctype="multipart/form-data">
            <p>Please upload profile picture:</p>
            <input type="file" name="photo"/>
            <input type="submit" value="Save"/>
            {{#if messages.length}}
            <ul class="validation">
              {{#each messages}}<li>{{this}}</li>{{/each}}
            </ul>
            {{/if}}
          </form>
          {{/unless}}
        <h3>Your services:</h3>
        <ul class="service-profile-list">
          {{#each services}}
          <div class="service-container">
            <li>
              <div class="service-name-container">
                <h4>{{this.name}}</h4>
              </div>
              <p>{{this.category}}</p>
              <p>{{this.price.amount}}€/{{this.price.unit}}</p>
              <p>{{this.description}}</p>
              <div class="edit-container">
                <a href="/profile/edit/{{this._id}}">EDIT</a>
              </div>
            </li>
          </div>
          {{/each}}
        </ul>
      <div class="adding-service">
        <h3>Would you like to <br> add a service?</h3>
        <div class="add-container">
          <a href="/profile/create-service">ADD SERVICE</a>
        </div>
      </div>
    </section>
  </div> 
</div>